{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn ecommerce.wsgi:application --bind 0.0.0.0:$PORT --workers 3"
  }
}